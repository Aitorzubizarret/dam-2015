HTMLFormElement.prototype.vaslidate = function() {
    var validador = {
        required : function() {

        },
        email : function() {

        }
    };

    var validate = function() {

    },

    var requeridos = this.querySelectorAll('.required');

    this.addEventListener('submit', validate, false);
};